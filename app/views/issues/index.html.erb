<p style="color: green"><%= notice %></p>

<h1>Issues</h1>

<div id="issues">
  <% @issues.each do |issue| %>
    <%= render issue %>
    <p>
      <%= link_to "Show this issue", task_issue_path(@task, issue)%>
    </p>
  <% end %>
</div>

<%= link_to "New issue", new_task_issue_path %>
